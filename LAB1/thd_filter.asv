clear
close all
clc
total_sample = 201;
f_out=fopen("thd_values.txt",'w');
for i=4:10
    s1="resultsC_";
    s2=int2str(i);
    filename=strcat(s1,s2);
    fi_n=fopen(filename,'r');
    x=zeros(total_sample);
    for j=1:201
        fscanf(fin,"%d");
    end    
    fclose(fin);
    thd=thd(x);
    fprintf(f_out,"n_bit = %d\t thd = %d",i,thd);
end
fclose(f_out);